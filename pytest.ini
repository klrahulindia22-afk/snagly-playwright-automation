[pytest]
addopts = -ra --strict-markers
testpaths = tests
markers =
    smoke: fast, critical-path checks
    auth: authentication checks
    boards: board-management checks
    regression: broader functional coverage
